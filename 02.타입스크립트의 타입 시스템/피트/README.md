## TS 스터디 3일차: 아이템 6~7

### 아이템 6: 타입스크립트 서버와 언어 서비스

에디터의 자동 완성, 오류 표시, 정의로 이동, 이름 변경이 에디터 자체가 아니라 백그라운드에서 동작하는 `tsserver`의 언어 서비스 덕분이라는 점을 알게 됐다.
`tsserver`와 `tsc`의 역할을 구분할 수 있게 됐다.
`tsserver`는 코드를 작성하는 동안 에디터에 빠른 피드백을 제공한다.
`tsc`는 프로젝트를 타입 검사하고 설정에 따라 JavaScript를 생성한다.
typeof null -> object
상위 3비트로 타입을 결정하는데, object와 null은 상위 3비트여서 구분하기 어렵다.

### 아이템 7: 타입을 값의 집합으로 보기

타입을 이름이나 속성 목록이 아니라 **할당 가능한 값의 집합**으로 생각하니 구조적 타이핑을 더 자연스럽게 이해할 수 있었다.
타입스크립트가 타입의 동일성보다 “이 값을 저 타입에 할당할 수 있는가?”를 자주 검사하는 이유도 집합의 포함 관계로 연결됐다.
`unknown`이 모든 JavaScript 값을 포함하는 최상위 타입이라는 사실도 집합 관점에서는 직관적이었다.
아이템 4에서 배운 “객체 타입은 정확히 그 속성만 가진 값이 아니다”라는 내용과도 이어졌다.
구조적 타이핑에서 객체 타입은 필요한 속성을 가진 값들의 집합이다.
따라서 필요한 속성 외에 다른 속성을 가진 값도 같은 집합에 포함될 수 있다.

#### 기존 직관이 뒤집힌 지점

##### 인터섹션은 왜 공통 속성만 남기지 않을까?

처음에는 교집합이므로 두 객체 타입의 공통 속성만 남을 것이라고 생각했다.
하지만 인터섹션은 **속성 이름의 교집합**이 아니라 **두 타입에 모두 할당 가능한 값의 집합**이다.
따라서 객체 값이 두 타입을 동시에 만족하려면 일반적으로 양쪽이 요구하는 속성을 모두 가져야 한다.

##### `extends`로 필드를 추가했는데 왜 값의 집합은 좁아질까?

인터페이스의 필드는 늘어나지만, 모든 필드를 만족해야 하므로 실제로 할당 가능한 값의 수는 줄어든다.
상속 관점에서는 타입을 “확장”하지만, 집합 관점에서는 기존 타입의 **부분집합**을 만드는 셈이다.
속성의 개수와 값 집합의 크기가 반대로 움직일 수 있다는 점이 새로웠다.

##### 타입 검사는 언제 일어날까?

언어 서비스는 코드를 작성하는 동안 할당 가능성을 계속 검사한다.
`tsc`도 컴파일 과정에서 타입을 검사하고 TypeScript를 JavaScript로 변환한다.
실제 런타임에는 타입 정보가 사라지고 변환된 JavaScript만 브라우저나 Node.js에서 실행된다.

#### 아직 쉽게 이해되지 않는 부분

##### 구조적 타이핑과 잉여 속성 체크의 관계

객체 리터럴을 직접 전달할 때 추가 속성 때문에 오류가 나면 구조적 타이핑이 적용되지 않는 것처럼 느껴졌다.
현재의 이해로는 구조적 타이핑이 꺼지는 것이 아니라, 새 객체 리터럴의 오타를 잡기 위한 **추가 검사**가 함께 적용되는 것이다.
같은 객체를 변수에 먼저 담으면 통과할 수 있다는 차이가 여전히 낯설어서 예제를 더 만들어볼 필요가 있다.

##### 집합 관점과 불변성의 관계

타입의 값 영역이 비슷해 보여도 제네릭이나 가변성 조건 때문에 할당 가능성이 달라질 수 있다는 점은 아직 명확하지 않다.
공변성, 반공변성, 불변성이 집합의 포함 관계를 어떻게 바꾸는지 별도로 확인하고 싶다.

#### 더 궁금한 점

`tsserver`와 `tsc`는 같은 타입 검사 결과를 어떤 방식으로 공유하며, 에디터에서는 어떻게 빠른 응답을 유지할까?
`unknown`이 최상위 타입이라면 반대로 아무 값도 포함하지 않는 `never`는 실제 코드에서 언제 유용할까?
background-color:: yellow
객체를 정확히 선언된 속성으로만 제한해야 하는 상황에서는 구조적 타이핑 위에 어떤 패턴을 사용해야 할까?

#### 이번 기록에서 남길 한 줄

타입을 값의 집합으로 바라보니 구조적 타이핑, 인터섹션, `extends`, `unknown`이 서로 다른 규칙이 아니라 할당 가능성과 집합의 포함 관계를 설명하는 하나의 관점으로 연결됐다.

## TS 스터디 4일차: 아이템 8~10

### 아이템 8: 타입 공간과 값 공간의 심벌 구분하기

클래스가 타입으로 쓰일 때는 형태(속성과 메서드)로 사용된다는 건 알겠는데, 값으로 쓰일 때는 생성자가 사용된다는 말이 무슨 말이지?
-> 값으로 사용된다는 말은 클래스가 new 연산자를 통해 생성되는 인스턴스로 사용된다는 말이다.
`typeof` 연산자는 뒤가 타입인지, 값인지에 따라서 다르게 동작하는구나
`typeof`, `[](속성 접근자)` -> 타입 연산자와 타입 간의 매핑이 이해가 잘 안된다.

> 핵심: TS에서는 타입인지 값인지 구분을 잘 해야 한다.

### 아이템 9: 타입 단언 대신 타입 선언 사용하기

타입 선언 > 타입 단언
그렇지만 타입 단언이 **반드시** 필요한 경우도 있네?
DOM을 조작하는 경우: TS는 DOM에 접근할 수 없구나!
문맥상 null이 될 가능성이 없다는 걸 안다면, 단언으로 null을 제외할 수 있음
null일 가능성이 있다면 옵셔널 체이닝(?) 사용을 편리하다.
(!)연산자와 비슷해 보이지만 다른 의미를 가진다.
옵셔널 체이닝 연산자는 **JS의 문법**이다!
단언이 만능은 아니다. 서로의 서브타입이 아닌 경우는 변환이 불가능하다.
이걸 해결하려면 unknown 사용, but 매우 찝찝.. 코드 위험...
`as const`: 타.입 단언이라기 보다는 타입을 더 구체적이고 안전하게 만드는 구문
어떨 때 사용하더라? -> 상수라고 주장하는 것

1. 단순히 리터럴을 사용하고 싶을 때
2. 객체를 읽기 전용으로 만들 때
3. 배열을 읽기 전용의 튜플로 만들 때
   타입을 명시하지 않는 경우

```ts
function createColourVariant(colour: Colour, variant: Variant) {
  return `${variant}-${colour}`;
}
```

TS는 반환값을 `string`으로 추론한다.
타입을 명시한 경우

```ts
type ColourVariant = `${Variant}-${Colour}`;
// "light-red" | "light-green" | "light-blue" | "dark-red" | ...

function createColourVariant(colour: Colour, variant: Variant): ColourVariant {
  return `${variant}-${colour}`;
}
```

`ColourVariant`라는 새 타입을 미리 정의해서, "가능한 조합 6개 중 하나"라고 명시적으로 알려줘요.
장점: 이 타입을 다른 곳에서도 재사용할 수 있음.
단점: 타입을 하나 더 만들어야 해서 코드가 늘어남.
`as const`로 단언한 경우

```ts
function createColourVariant(colour: Colour, variant: Variant) {
  return `${variant}-${colour}` as const;
}
```

`as const`는 "이 값을 넓은 `string`이 아니라, 가능한 한 가장 좁고 정확한 타입으로 취급해줘"라는 의미를 가진다.
그러면 TypeScript가 별도의 `type ColourVariant` 없이도 자동으로 `"light-red" | "light-green" | ... ` 같은 정확한 타입을 추론한다.
어떻게 이게 가능한 걸까?
템플릿 리터럴 타입 안에 유니온 타입이 존재하면, TypeScript는 가능한 모든 조합을 자동으로 곱해서 새로운 유니온 타입을 만든다.
이건 TS의 템플릿 리터럴 타입 시스템의 기능이다. (일반 JS 문자열 템플릿과 달리, 타입 레벨에서 조합을 계산해줌)

### 아이템 10: 객체 래퍼 타입 피하기

우리가 기본형에 대해 사용하던 그 타입들(`String`, `Number`, ...)이 맞다.
기본형을 각자의 객체로 래핑해서 래퍼 타입이라고 하는구나!
JS의 프로토타입과 관련이 있을까?
기본형 타입은 객체 래퍼에 할당할 수 있다. 기본형 타입을 객체 래퍼에 할당하는 선언을 허용한다. <- 이 말이 어렵다. 쉽게 설명하면 뭘까?
기본형 타입을 객체 래퍼에 할당한다는 의미:

```ts
// 문제 없는 코드!

let str: string = 'hello';
let strObj: String = str;
```

반대로 객체 래퍼를 기본형에 할당한다는 의미:

```ts
// 오류!

let strObj: String = new String('hello');
let str: string = strObj;
```

왜 헷갈리기 쉽다는 말을 할까?

```ts
function greet(name: String) {
  // 실수로 대문자로 씀
  console.log(`Hello, ${name}`);
}

greet('Alice'); // 동작하긴 하는데..
```

()기본형 타입 -> 객체 래퍼) 할당이 가능하므로 대문자 `String`을 타입으로 써도 문법적으로 에러 X
근데 `String` 객체는 `.length`, `.toUpperCase()` 같은 메서드는 있어도, 원시값처럼 `===` 비교가 제대로 안 되거나 예상치 못한 동작을 할 수 있다.
결론은 그냥 소문자 써라.

## TS 스터디 5일차: 아이템 11~12

### 아이템 11: 타입 체크와 잉여 속성 체크 구분해서 사용하기

**잉여 속성 체크와 할당 가능성 체크는 별도의 과정이다?**

**잉여 속성 체크는 왜 수행되어야 할까?**

의도와 다르게 작성된 코드를 찾으려고 하기 때문. 순수한 구조적 타입 체커는 오타를 잡을 수 없다 (구조적 타이핑으로 인해).

**객체 리터럴인 경우에만 잉여 속성 체크를 수행한다.**

> 한 줄 요약: 잉여 속성 체크는 속성명의 오타와 같은 실수를 잡는 데 효과적이다.

### 아이템 12: 함수 표현식에 타입 적용하기

**함수의 매개변수부터 반환값까지 전체를 함수 타입으로 선언해 함수 표현식에 재사용할 수 있다는 장점이 있다 ← 이게 어떤 의미일까?**

```ts
// 1. 함수 문장
// a, b, 반환값의 타입을 계속 일일히 작성해줘야 한다.
function add(a: number, b: number): number {
  return a + b;
}

// 2. 함수 표현식
// 함수 자체를 타입으로 만들어서, 재사용이 가능하다.
type MathOp = (a: number, b: number) => number;

const add: MathOp = (a, b) => a + b;
const subtract: MathOp = (a, b) => a - b;
```

반복되는 함수 시그니처를 하나의 함수 타입으로 통합하는 데 의미가 있다. 재활용!

**라이브러리들은 공통 함수 시그니처를 타입으로 제공하기도 한다.**

fetch 타입이 있다는 사실은 처음 알았다! TS가 input과 init의 타입을 추론할 수 있게 한다. (fetch 메서드에 `typeof`를 붙여 타입으로 가져온다)

**매개변수는 기존 걸 사용하면서, 반환 타입만 변경하고 싶은 경우 `Parameters` 유틸리티 타입을 사용하자.**

반대로 반환 타입만 가져오고 싶다면 `ReturnType` 유틸리티 타입을 사용하자.

**`Parameters` 유틸리티 타입을 사용하는 경우가 뭐가 있을까?**

1. 책에서의 예시: 매개변수 타입만 사용하고 싶을 때
2. 래퍼 함수를 만들고 싶을 때

```ts
function log(...args: Parameters<typeof console.log>) {
  console.log('[LOG]', ...args);
}
```

`console.log`가 어떤 매개변수를 받는지 몰라도, `Parameters<typeof console.log>`로 그 타입을 그대로 가져와서 안전하게 위임할 수 있음.

3. 이벤트 핸들러나 콜백 타입을 재사용하고 싶을 때

## TS 스터디 6일차: 아이템 13~15

### 아이템 13: 타입과 인터페이스의 차이점 이해하기

`type`, `interface` 두 차이를 명확하게 알 필요가 있다.

작동 방식은 두 방식이 거의 동일하다.

제네릭, 함수 시그니처, 구현(implements), 재귀, ...

#### 차이점

유니온 타입은 존재하지만, 유니온 인터페이스라는 개념은 없다.

```ts
type AorB = 'a' | 'b'; // 가능
interface AorB = 'a' | 'b'; // 불가능
```

#### 책의 내용을 기반으로 타입 별칭이 어떨 때 의미가 있을까?

유니온 타입을 정의할 때

튜플을 만들 때

객체가 아닌 것(원시값, 함수)에 이름 붙일 때

... 등등

#### 그럼 interface에서만 제공하는 보강 기능은 어떨 때 의미가 있을까?

선언 병합이라고도 함

보통 타입을 확장할 때(특히 라이브러리의 타입들) 사용하고, 전역 타입에 속성을 추가하는 경우에도 사용한다.

#### d.ts와 같은 산출물에 type과 interface는 다르게 작동한다.

인라인인 경우: type은 구조로 보기 때문에 이름을 몰라도 괜찮지만, interface는 이름으로 참조하려고 하기 때문에 에러가 발생한다.

### 아이템 14

JS의 기본형은 불변이므로 값을 변경하는 메서드가 없는데, 배열과 객체는 가변이므로 readonly 접근자가 의미가 있다.

그러나 `readonly` 접근자나 `Readonly<T>` 제네릭 유틸 타입은 얕게 작동한다.

#### 우리가 자주 실수하는 부분이 뭐가 있을까?

책에서의 예시도 있고, 배열의 원소를 실수로 접근한 경우도 같은 문제가 발생한다.

```ts
interface Todo {
  text: string;
  done: boolean;
}

function printTodos(todos: readonly Todo[]) {
  todos.push({ text: 'new', done: false }); // ❌ 에러! push는 배열 자체를 변경하니 막힘

  todos[0].done = true; // ✅ 통과됨! 하지만 원본 데이터가 실제로 바뀌어버림
}
```

매개변수와 readonly로 바꿀 때의 이슈가 존재할 수 있지만, 보통은 함수의 매개변수를 변경하지 않는다는 가정을 하는 경우가 많다. -> 이 실수를 줄여야 함

### 아이템 15

제네릭은 추상화를 제공하는 것뿐만 아니라, 사용자의 실수를 방지한다.

타입이 중복이 더 흔한 이유는 덜 익숙하기 때문이다.

타입의 중복을 줄이기 위해서 유니온을 사용하거나 인덱싱을 할 수 있다.

매핑된 타입? -> 루프 방식으로 동작

메서드에 `ReturnType` 제네릭을 사용해서 함수의 반환값을 유추할 수 있다.

추상화 과정에서 타입의 이름을 짓기 어렵다면 해당 리팩터링은 불필요할 수 있다.

> 잘못된 추상화보다 중복이 훨씬 낫다!!!

## TS 스터디 7일차: 아이템 16~17

### 아이템 16: 인덱스 시그니처보다 정확한 타입 사용하기

인덱스 시그니처는 객체 타입을 정의할 때 부정확할 수 있다.

인덱스 시그니처는 동적 데이터를 표현할 때 사용된다?

?

잉여 속성 체크를 우회하기 위해서 인덱스 타입을 사용할 수 있다.

?

### 아이템 17: 숫자 인덱스 시그니처 지양하기

악명 높은 동작 -> 암시적 타입 강제

JS에서 객체의 키는 문자열이다.

TS에서 숫자 키를 허용한다. 문자열 키와 다르다.

#### ArrayLike 타입을 왜 사용할까?

Iterable 타입?

JS와 TS의 객체 키 값은 문자열이거나 심벌이다. 숫자 형태는 문법적 허용일 뿐, 가능하면 타입 정의에 사용하지 않는 것이 이상적이다.

## TS 스터디 8일차: 아이템 18~19

### 아이템 18: 추론 가능한 타입을 사용해 장황한 코드 방지하기

#### 내용 정리

TS에서 매번 타입을 선언하는 것은 비생산적이다 -> 타입 추론!

복잡한 객체도 추론할 수 있다.

구조분해 할당을 통해 각 지역 변수가 알아서 추론되도록 하는 것이 바람직하다.

주의할 점: 구조 분해 할당 내부에 타입을 직접 지정할 수 없음 <- 이름 변경의 의미로 알아먹기 때문에

메서드에 반환 타입을 명시하면, 내부 구현의 오류가 사용자 코드의 오류로 표시되지 않는다.

한 마디로 오류 위치를 정확하게 표시해준다.

반환 타입을 명시해야 하는 가장 큰 이유는 직관적인 표현 및 의도를 드러낸다는 점이다.

#### 궁금한 점

정보가 부족해서 스스로 타입을 추론하기 어려운 상황은 뭐가 있을까?

매개변수인 경우, 초기값이 없는 변수/배열, 외부 데이터, ...

any타입으로 추론된다.

### 아이템 19: 다른 타입에는 다른 변수 사용하기

중요한 점: 변수의 값은 바뀔 수 있지만, 그 타입은 보통 바뀌지 않는다.

#### 타입 지정 방법?

```ts
let value; // 처음엔 any처럼 보이지만...

if (Math.random() > 0.5) {
  value = 'hello';
  // 여기서 value: string
} else {
  value = 123;
  // 여기서 value: number
}

// 함수 밖으로 나오면
value; // value: string | number  ← 타입이 넓어짐
```

타입의 진화는 어디까지나 예외이지, 규칙이 아님

유니온을 쓰면 해결되지만, 다루기 더 어려워진다.

차라리 별도의 변수를 사용하자!

> let보다 const가 더 많아야 한다!!

타입이 바뀌는 변수를 지양해야 하기 때문

그렇다고 변수명이 같은데 타입이 다르면 사람에게 혼란을 줄 수 있다.

#### no-shadow 린터 규칙?

```ts
let x = 'outer';

function foo() {
  let x = 'inner'; // 바깥의 x를 "가림" (shadowing)
  console.log(x); // "inner"
}

foo();
console.log(x); // "outer" (바깥 x는 안 바뀜)
```

위 코드는 사용자에게 혼란을 줄 수 있음.

```ts
let x = 'outer';

function foo() {
  let x = 'inner'; // ❌ ESLint 에러: 'x' is already declared in the upper scope
}
```

린터 규칙을 통해 에러를 명시적으로 보여주기!
